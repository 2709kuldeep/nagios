define servicegroup {
       servicegroup_name                        nagiosclients-http
       alias                                    nagiosclient-http
       members                                 	nagiosclient1, httpd port status, nagiostest, httpd port status 
}

